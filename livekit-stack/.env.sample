# LiveKit Stack Environment Variables
# Copy this to .env and fill in your values
# NEVER commit .env to git!

# ============================================
# LiveKit API Credentials
# ============================================
# Generate with: openssl rand -base64 32
LK_API_KEY=your_livekit_api_key_here
LK_API_SECRET=your_livekit_api_secret_here

# ============================================
# TURN Server Credentials
# ============================================
TURN_DOMAIN=rtc.pizoo.app
TURN_USER=pizoo
# Generate with: openssl rand -base64 24
TURN_PASS=your_strong_turn_password_here

# ============================================
# Redis (Optional but recommended)
# ============================================
# Generate with: openssl rand -base64 32
REDIS_PASSWORD=your_redis_password_here

# ============================================
# Domain Configuration
# ============================================
DOMAIN=rtc.pizoo.app
ADMIN_EMAIL=admin@pizoo.app

# ============================================
# VPS Configuration
# ============================================
# Your VPS public IP address
# VPS_PUBLIC_IP=1.2.3.4

# ============================================
# Monitoring (Optional)
# ============================================
# PROMETHEUS_ENABLED=true
# GRAFANA_ENABLED=true
# GRAFANA_PASSWORD=your_grafana_password

# ============================================
# Notes
# ============================================
# 1. Generate strong passwords using: openssl rand -base64 32
# 2. Keep this file secure (chmod 600 .env)
# 3. Never commit .env to version control
# 4. Update these values in your backend .env as well:
#    LIVEKIT_URL=wss://rtc.pizoo.app
#    LIVEKIT_API_KEY=<same as LK_API_KEY>
#    LIVEKIT_API_SECRET=<same as LK_API_SECRET>
